#ifndef AREAATTRIBUTES_H
#define AREAATTRIBUTES_H
#include "stdafx.h"

enum AreaType
{
    AT_COMBAT,
    AT_NON_COMBAT,
    AT_SAFE,
    AT_TOTAL
};

enum SurfaceMaterial
{
    SM_CONCRETE,
    SM_METAL,
    SM_TOTAL
};

#endif